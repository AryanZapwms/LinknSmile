# Multi-Country Launch — Outstanding Requirements

> Purpose: this is a parking lot for everything the UAE/Qatar/Saudi expansion needs that is **not** an engineering task — business decisions, account signups, documents, and approvals that have to come from your boss or from external providers before certain code can be finished or tested. Engineering work that doesn't depend on any of this keeps moving in parallel (see "Not Blocked" section at the bottom).
>
> Last updated: 2026-08-24 (i18n — email templates fully translated, `User.locale` infrastructure built and kept in sync on every locale-switch, see `PROJECT_SOURCE_OF_TRUTH.md` §4.25 — item 3; i18n/RTL Batch 5 — toast-string sweep completed, validation-message question resolved (no work needed) — item 3; Arabic-Indic OTP digit-input bug fixed, standalone fix, see `PROJECT_SOURCE_OF_TRUTH.md` §4.23 — item 3; i18n/RTL Batch 4 — auth/registration forms — completed — item 3; i18n/RTL Batch 3b — vendor portal complete — item 3; i18n/RTL Batch 3 — vendor portal, first slice — completed — item 3; i18n/RTL Batch 2 — core purchase funnel — completed — item 3; i18n/RTL Phase 1 proof-of-concept completed — item 3; tax/VAT engine computation piece completed — item 2; Tap Payments still paused on phone verification — item 1)

---

## 1. Tap Payments (Payment Gateway — UAE/Qatar/Saudi)

**Status:** Code complete (gateway abstraction, schema, checkout/subscription flows, redirect handling) and typechecked. **Not yet live-tested** — blocked on account signup.

### What's blocking it
- Tap's signup form (`register.tap.company`) requires a mobile number from a supported GCC/MENA country (Kuwait, Bahrain, Egypt, Jordan, Lebanon, Oman, Qatar, Saudi Arabia, UAE). No Indian number accepted.
- Google/Facebook sign-in shortcut is currently broken on Tap's own side (`Error 400: origin_mismatch` — their OAuth config issue, not ours).

### What's needed, and from whom
| Item | Needed from | Notes |
|---|---|---|
| A mobile number from any supported country | **Boss** — one of his existing UAE/Qatar/Saudi contacts | Just needs to receive one OTP to complete signup. Doesn't need to be a permanent business number. |
| Sandbox `TAP_SECRET_KEY` | Self-serve once signup completes | No business KYC needed for test-mode key — confirmed self-serve, key-based, test keys prefixed `sk_test_`. |
| (Later, for going live) Business KYC — trade license, bank IBAN, signed activation letter | **Boss** — business/legal side | Only needed before *real* money can move. Not needed to build/test in sandbox. |
| A non-production test MongoDB database | Can set up independently (free MongoDB Atlas tier) | So sandbox Tap testing doesn't touch real India order data. |

### What happens once unblocked
1. Get sandbox `sk_test_...` key from Tap dashboard (goSell → API Credentials → Generate Key)
2. Point local `.env.local` at a test database, add `TAP_SECRET_KEY`, set `PAYMENT_GATEWAY=tap`
3. Run a real end-to-end test: create order → Tap hosted redirect → test card → return → verify → order created
4. Spot-check `app/vendor-tap-return/page.tsx`'s auth handling once (it sits outside the normal `app/vendor/**` layout gating for a documented reason — worth one look before trusting it)

---

## 2. Tax / VAT Engine

**Status: computation/charging piece DONE (2026-08-21).** Rates confirmed and implemented: UAE 5% flat, Saudi Arabia 15% flat, Qatar 0% (confirmed still genuinely not implemented there as of 2026, not a placeholder). `computeOrderPricing` (`lib/pricing.ts`) now applies `PlatformSettings.taxRatePercent` additively on the post-discount subtotal; vendor commission/payout math is unaffected (computed pre-tax, exactly as before); `Order.taxRatePercent`/`taxAmount` record what was actually charged; the cosmetic "Tax: Included" labels in cart/checkout are gone, replaced with a real computed line. Threaded through every route that touches pricing, including Tap's (the gateway that will actually see non-zero rates in production). Live-verified — India confirmed as a genuine no-op, a 5% test order confirmed correct end-to-end, all test data cleaned up. Full detail: `PROJECT_SOURCE_OF_TRUTH.md` §4.17.

**Money model** (confirmed explicitly before implementing, given real legal stakes): tax is additive on the post-discount subtotal, charged to the customer, doesn't touch vendor commission/payout math — the platform collects it and is responsible for remitting, consistent with KSA/UAE "deemed supplier" marketplace VAT treatment (the platform, not individual vendors, is the party obligated to register/collect/remit in that framework). This is a computation-and-charging model, **not verified tax/legal advice** — whether this business's actual KSA/UAE operations trigger deemed-supplier registration or other compliance obligations beyond charging the right number is still a real question for actual counsel, not something this code change answers.

### Still open — deliberately out of scope for the 2026-08-21 work
| Item | Needed from | Notes |
|---|---|---|
| Whether VAT is per-category or flat-rate | **Boss** | Implemented as flat-rate only (one rate per deployment). If any product category needs zero-rating/exemption within a country that otherwise has VAT, that's a real follow-up task, not yet built. |
| Whether tax registration numbers need to be collected from vendors per-country (like the existing India-only `gstNumber` field) | **Boss** | Not built — explicitly out of scope per this task's own requirement 5 (no VAT registration collection, no invoicing/e-invoicing compliance features). |
| Actual KSA/UAE deemed-supplier compliance verification (registration thresholds, filing obligations) | **Boss / real tax counsel** | The code assumes/matches deemed-supplier treatment computationally but this was never confirmed as this business's actual legal status — see money model note above. |
| Vendor-facing tax reporting/invoicing | **Future task** | Explicitly excluded from this pass. |

### Not blocked on anything
The remaining items above are compliance/business-process work, not engineering blockers — nothing about them prevents launching in a country once the rate itself is confirmed (already done for all three).

---

## 3. i18n / RTL (Language Support)

**Status: Phase 1 (infrastructure + proof-of-concept) DONE (2026-08-21). Batch 2 (core purchase funnel, 8 files) DONE (2026-08-21). Batch 3 + 3b (vendor portal, complete — 16 files) DONE (2026-08-21). Batch 4 (auth/registration forms, 7 files) DONE (2026-08-24). Batch 5 (validation messages, toasts) DONE (2026-08-24) — toast-string sweep complete, validation-message question resolved (no translation needed). Email templates DONE as a follow-up (2026-08-24) — `User.locale` infrastructure built, all 8 customer/vendor-facing templates translated and RTL-verified, see §4.25. Batch 6 (static/policy pages — 6 files) DONE (2026-08-25), see §4.26. Batch 7 (profile pages + wishlist finish — 6 files) DONE (2026-08-25), see §4.27. Batch 7b (misc tier — categories/shop/sellers/blog chrome, homepage components, Tap-return pages, error/not-found, seller-onboarding pages — 15 files) DONE (2026-08-25), see §4.28 — **THE FINAL NON-ADMIN i18n BATCH. Current total: 63/81. Every one of the remaining 18 files was individually read and confirmed to need no translation work** (dead code, pure logic/provider components, framework plumbing, a dev-tooling demo page, and one file with a genuine technical constraint) — **none is outstanding translation work. Of the non-admin surface an actual customer, vendor, or seller would see, 100% is now translated.** Only `app/admin/**` remains, deliberately deferred per the standing decision (internal-only, lowest priority). See §4.28 for the full per-file accounting and an honest explanation of why 81 was never really the true ceiling.**

**What's done:** library chosen and verified (next-intl, cookie-based "without i18n routing" mode — deliberately not the standard URL-prefixed tutorial pattern, since restructuring ~150 routes under `app/[locale]/` would be a large migration disconnected from translation work and doesn't suit Path B anyway), message-catalog structure created (`messages/en.json`, `hi.json`, `ar.json`), RTL confirmed working via Tailwind v4's native logical properties (no plugin needed), a locale switcher built. Phase 1 fully translated and RTL-audited 3 representative files (`components/header.tsx`, `components/footer.tsx`, `app/page.tsx`); Batch 2 extended this to the core purchase funnel — 8 files — and added ICU plural handling with full Arabic CLDR plural categories, live-verified interactively (browse → detail → cart). Batches 3 and 3b together fully translated and RTL-audited the entire vendor portal — 16 files: layout/nav, dashboard, products (list/add/edit/bulk-upload), orders (list/detail), wallet (the largest single file in the sweep so far, 843 lines), payouts, bank details, reviews, settings, coupons (list/add/edit) — with the same ICU-plural approach, plus deep scrutiny on commission/payout/subscription/wallet terminology (the entire vendor portal is money-adjacent, so this carried real trust risk, not just UX polish) and, for batch 3b specifically, backend enum values were read directly from `lib/models/ledger.ts`/`lib/models/payout.ts` rather than guessed from the UI, to make sure ledger/payout status labels are actually correct rather than just plausible-looking. Batch 4 translated and RTL-audited the full auth/registration flow — login, register (customer + vendor), OTP verification, forgot/reset password — 7 files, with careful handling of the OTP/phone numeric-entry RTL edge case (forced `dir="ltr"` on those specific inputs) and, since every route in this batch is reachable without authentication (unlike the vendor portal), full live browser + screenshot verification rather than the batches 3/3b static-substitute approach. New env var: `NEXT_PUBLIC_SECONDARY_LOCALE` (e.g. `"hi"` for India, `"ar"` for UAE/Qatar/Saudi) — empty/unset means English-only, a no-op for the current deployment. Full technical detail: `PROJECT_SOURCE_OF_TRUTH.md` §4.18 (Phase 1), §4.19 (Batch 2), §4.20 (Batch 3), §4.21 (Batch 3b), §4.22 (Batch 4).

**Translation confidence, resolved without needing a translator for this pass:** both Arabic and Hindi got real translations (not placeholders) for everything translated so far — every string in scope was short-to-medium common e-commerce/vendor-portal/auth-form UI/nav/marketing/toast/validation copy, not specialized or legally-sensitive content, so this was judged in-scope confidence. **Still recommend a native-speaker review pass on `messages/hi.json` and `messages/ar.json` before either goes live** — this wasn't certified by a native speaker, just judged reasonable-confidence machine+model translation. This now specifically includes: the Arabic ICU plural word-forms added in Batch 2 (e.g. `منتج`/`منتجان`/`منتجات`/`منتجًا`); Batch 3's commission/payout/subscription/wallet terminology; **Batch 3b's ledger/accounting-specific vocabulary most of all** (Hindi `लेजर`/`समाशोधन`/`निपटान`, Arabic `دفتر الحسابات`/`تسوية`, and the deliberate `VOIDED`-vs-`CANCELLED` word split in both languages) — this remains the most specialized, least "ordinary UI copy" vocabulary translated in the sweep so far; and Batch 4's GST/PAN field labels, which are translations of India-only concepts onto a form GCC vendors will also see (see the two new items below — this is a translation-quality note, distinct from the already-tracked regional-fit question).

**Two new items surfaced during Batch 4, neither blocking anything:**
- **Stale "Instapeel" branding + a genuinely broken image** on `app/auth/forgot-password/page.tsx` — the left brand panel still says "Instapeel" / "Beauty & Skincare Excellence" instead of "Linknsmile", and its logo image (`public/companylogo.jpg`) doesn't exist on disk at all, producing a broken image and a `400` from Next's image optimizer on every load of this page. Pre-existing, unrelated to i18n — flagged because it surfaced during this batch's live-verification screenshots. **Still open** — someone with the correct current branding/logo asset needs to fix this; not an i18n task.
- **Arabic-Indic digit input bug** in both OTP entry fields (`otp-form.tsx`, `verify-otp/page.tsx`) — flagged in Batch 4, **fixed in a dedicated follow-up session (2026-08-24)**. Two separate root causes: `verify-otp/page.tsx` actively stripped Arabic-Indic numerals (٠-٩) via a Western-digit-only regex; `otp-form.tsx` had no digit filtering at all, so Arabic-Indic input would pass through unnormalized and fail backend comparison. Fixed with a shared `lib/normalize-digits.ts` helper that converts Arabic-Indic digits to standard digits before the existing digit-only filter runs, in both locations. Live-verified with real typed Arabic-Indic keystrokes in a Playwright session (not just code inspection) — see `PROJECT_SOURCE_OF_TRUTH.md` §4.23 for full detail.

**Scoping question raised during Batch 2, now resolved for the vendor side:** the vendor portal (`app/vendor/**`) is used by real vendors — actual UAE/Qatar/Saudi business owners — so it likely needs translation before a GCC launch, unlike the admin panel (`app/admin/**`), which is the platform's own internal team and can plausibly stay English-only indefinitely. **The vendor portal is now fully translated (batches 3 + 3b close it out).** The admin panel remains deliberately untouched, per the standing decision. **Auth/registration forms are also now fully translated (Batch 4).**

**Batch 5 findings — validation messages need no translation work, toast sweep was much smaller than estimated, email templates need a decision before proceeding:**
- **`lib/validation.ts`:** investigated, not translated. Two of its three zod schemas are dead code (unused anywhere); the third (`registerSchema`) is used purely server-side and, on failure, returns a raw error array that the client can't even render as text today (a separate pre-existing bug, not fixed here) — and is barely reachable anyway since the client already runs equivalent, already-translated validation first. No translation-worthy text actually reaches a user here. Full reasoning: `PROJECT_SOURCE_OF_TRUTH.md` §4.24.
- **Toast strings:** of the ~15-17 non-admin files using a toast system, **16 already had their strings translated** as part of batches 2/3/3b's page-level work — the original ~86-call estimate was never recounted after that work landed. Only `app/(customer)/wishlist/page.tsx` (3 strings, new `WishlistPage` namespace) and one missed fallback string in `app/vendor/reviews/page.tsx` needed anything. Both done, live-verified, typechecked clean.
- **New bug found while mapping toast call sites, flagged not fixed:** `sonner`'s `<Toaster>` is never mounted anywhere in the app (confirmed across all four `layout.tsx` files) — every `toast()` call from `import { toast } from "sonner"` (14 files) is currently a silent no-op in production, nothing renders, in any locale. Translated the strings anyway since that's correct regardless of the rendering bug, but this needs its own fix (likely just mounting `sonner`'s `<Toaster>`, or standardizing on the one toast system already working) — see decision table below.
- **Email templates (`lib/email.tsx`, `lib/EmailOtp.ts`): DONE as a follow-up session (2026-08-24), after confirmation of the proposed approach plus one refinement.** `User.locale` is captured at registration (both customer and vendor OTP flows, plus Google OAuth) and **kept in sync on every subsequent locale-switch by a logged-in user** — the requested refinement, so a user who registers in English but later switches to Arabic still gets emails in Arabic. All 8 customer/vendor-facing email builders translated (admin-only ones stay English, including one — `getPayoutRequestedEmail` — that's misleadingly named but confirmed sent only to the platform's own inbox). Two real bugs found and fixed during verification: (1) next-intl rejects literal HTML tags in a plain translation call — a runtime-only error invisible to typechecking, fixed by switching 5 affected keys to the rich-text API; (2) formatted dates and copyright lines could visually reorder when embedded in Arabic text — fixed by explicit left-to-right isolation, same category as the OTP-digit-direction fix. Full reasoning and verification detail: `PROJECT_SOURCE_OF_TRUTH.md` §4.25.

**Batch 6 (2026-08-25) — static/policy pages, plus a direct recount of the whole sweep:** the file count was recomputed from scratch rather than trusted (`grep -l "useTranslations\|getTranslations"` across 81 directly-measured in-scope non-admin `.tsx` files) — this found the prior "34 files" figure had drifted to actually 36 already done, most likely two files (`wishlist`, `order-success/[id]`) that picked up translations during Batch 5's toast-string work without the running total being updated at the time. 6 more files translated this batch — `termsofservice`, `privacy-policy`, `refund-policy`, `orders-and-returns`, `about-us`, `contact-us` — bringing the total to **42/81**. The 4 legal-adjacent pages (terms, privacy, refund policy, orders & returns) contain real legal/policy prose, not ordinary UI copy, so **every translated string in those 4 pages' `hi.json`/`ar.json` entries needs professional legal/native-speaker review before launch** — a stronger bar than the standing UI-copy review recommendation below, called out separately because getting legal text subtly wrong in a language that wasn't verified carries real risk, not just a UX rough edge. Confirmed `contact-us`/`about-us` were NOT already covered by the pre-existing `usePlatformSettings()` hook usage on those pages — that hook only supplies 3 dynamic contact values, an entirely separate concern from translation; both pages' UI copy was still 100% hardcoded. One small pre-existing bug (a duplicated contact-list entry in `about-us`, unrelated to translation) fixed in passing and documented. Full detail: `PROJECT_SOURCE_OF_TRUTH.md` §4.26.

**Follow-up (2026-08-25):** `privacy-policy`'s "WooCommerce" reference (flagged-not-fixed above) has been fixed — the false platform claim in `PrivacyPolicyPage.section3Content` was replaced with an accurate statement about this platform's actual service providers (Razorpay/Tap Payments for payments, MongoDB Atlas/Cloudinary for hosting), in `en.json`/`hi.json`/`ar.json`. This is a good-faith factual correction, not a legally-verified one — it's covered by the same legal-review requirement already stated above for this page, not a separate item. Full detail: `PROJECT_SOURCE_OF_TRUTH.md` §4.26.

**Batch 7 (2026-08-25) — profile pages, wishlist finish, auth-chrome gap resolved:** all 5 `app/profile/**` pages (settings, orders list, order detail, favourites, layout/nav) plus the remaining ~90% of `app/(customer)/wishlist/page.tsx` that Batch 5 hadn't reached (Batch 5 only translated its 3 toast strings, not the actual page UI) plus the shared `FavouriteButton.tsx` component (its accessibility label) — 6 files, bringing the total to **48/81**. The two "auth page-chrome gap" files flagged at the end of Batch 6 (`app/auth/login/page.tsx`, `app/auth/register/page.tsx`) turned out to be 9-line wrappers with zero hardcoded text — confirmed by reading both files, nothing to translate, not a real gap, just a heuristic false positive (the file-count method flags absence of `useTranslations`, not presence of untranslated text). Order-status and payment-status labels across the orders list and order-detail pages got the same careful terminology treatment as the vendor wallet work, including preserving an existing (pre-batch, unrelated to translation) cross-page inconsistency where the list view shows friendly status labels but the detail view shows raw backend values — flagged, not fixed, since it's not a translation issue. Full detail: `PROJECT_SOURCE_OF_TRUTH.md` §4.27.

**Batch 7b (2026-08-25) — the misc tier, and the final non-admin batch:** per explicit instruction, all 33 raw-remaining candidate files were individually read and triaged (not assumed real or assumed false-positive) before translating anything. Result: **15 files were real work, 18 were not** — a notably higher false-positive rate than Batch 7's 2-file surprise. The 15 translated: `categories/[slug]`, `shop/[slug]`, `sellers`, `blog` list + detail (chrome only), `why-choose.tsx`, `home-carousel.tsx`, `category-slider.tsx` (the Phase 1 proof-of-concept had explicitly marked this one out of scope for that batch — re-confirmed now as real and in-scope), `cart-icon.tsx`, `checkout/tap-return` + `vendor-tap-return` (Tap Payments return pages), `error.tsx`, `not-found.tsx`, `register-as-seller`, `vendor-apply`. Notably, 5 of these (`categories/[slug]`, `shop/[slug]`, `sellers`, both `blog` pages) turned out to be Server Components — the first time this whole sweep needed `getTranslations()` from `next-intl/server` instead of the client-side `useTranslations()` hook used everywhere else; it worked cleanly with no changes needed to the existing i18n infrastructure. **New discovery: 4 components confirmed as dead code** (`FAQ.tsx`, `testimonials.tsx`, `brand-filters.tsx`, `coming-soon.tsx` — verified unreferenced anywhere in the codebase), all containing stale pre-rebrand skincare-brand marketing content — flagged as a cleanup opportunity for whoever owns them, not translated (translating dead code with stale branding would have been waste twice over). **Final tally: 63/81** — and critically, every one of the remaining 18 files was individually confirmed to need no translation work (see `PROJECT_SOURCE_OF_TRUTH.md` §4.28 for the complete per-file breakdown: 2 already-known non-issues, 2 newly-formally-confirmed, 4 dead code, 6 pure logic/provider files, 1 by-design exclusion, 1 fully-dynamic-content component, 1 technical constraint, 1 dev-tooling page). **81 was never the true ceiling of files needing translation** — it was a raw candidate count including dead code and framework plumbing. Of the non-admin, customer/vendor/seller-facing surface, translation work is complete. One real bug found and fixed along the way: `app/not-found.tsx`'s embedded shop link used the wrong `next-intl` interpolation pattern (a bare `{shopLink}` placeholder instead of `<shopLink>shop</shopLink>` tag syntax) — fixed and live-verified rendering correctly in Arabic. Full detail: `PROJECT_SOURCE_OF_TRUTH.md` §4.28.

### What's needed, and from whom — now narrower than before
| Item | Needed from | Notes |
|---|---|---|
| Which languages, precisely | **Boss** | Still open. India got Hindi, UAE/Qatar/Saudi got Arabic as the working assumption — confirm this is actually the intended pairing, and whether English-only is acceptable at launch for any of the GCC markets. This no longer blocks any remaining file-sweep work — the non-admin sweep is complete — but still needs an answer before launch. |
| ~~Decision: how should the system determine which language to send a given email in?~~ | — | **Resolved and implemented (2026-08-24).** `User.locale`, captured at registration, kept in sync on every locale-switch, `"en"` fallback. See `PROJECT_SOURCE_OF_TRUTH.md` §4.25. |
| ~~The remaining non-admin file sweep~~ | — | **Resolved — the non-admin i18n sweep is complete as of Batch 7b (2026-08-25), see `PROJECT_SOURCE_OF_TRUTH.md` §4.28.** Final count: 63/81 directly-measured in-scope files translated; every one of the remaining 18 was individually confirmed to need no translation work (dead code, pure logic/provider components, framework plumbing, a technical constraint, a dev-tooling demo page) rather than left untriaged. 4 dead-code files were newly discovered and flagged for cleanup (not translated) — see the item below. Only `app/admin/**` remains, deliberately deferred, lowest priority, internal-only. |
| **New (2026-08-25):** cleanup opportunity — 4 unreferenced dead-code components with stale pre-rebrand branding | **Engineering (whoever owns cleanup), low priority** | `components/FAQ.tsx`, `testimonials.tsx`, `brand-filters.tsx`, `coming-soon.tsx` — confirmed via direct grep to be imported nowhere in the codebase. All contain leftover skincare-brand content (Instapeels/Dermaflay/Vibrissa product FAQs and testimonials, or generic "beauty and skincare" copy) from before the platform's pivot to a general marketplace. Not translated (would have been pure waste on dead code) and not deleted (out of scope for an i18n pass) — flagged here as a genuine, low-priority cleanup candidate. |
| **Future consideration, explicitly out of scope for this infrastructure sweep:** should blog post *content* eventually support multi-language authoring? | **Boss / product decision** | Raised during Batch 7's scoping, not resolved or acted on. `app/blog/**`'s page chrome (nav labels, "read more", date formatting, etc.) is ordinary translatable UI copy like any other page and belongs in the 7b misc-tier sweep. The actual blog post body/title/content, however, is admin-authored data stored in the database, not UI copy — translating it isn't a mechanical extraction-into-a-catalog job like everything else in this sweep, it's a genuine content-authoring/CMS question (does an admin write each post once in English only, or in every supported language? is there a translation workflow?). Flagged here as a distinct future product question, deliberately not answered or designed in this i18n infrastructure sweep. |
| Native-speaker review of `hi.json`'s newly-added email content | **Boss / translator** | Same standing recommendation as the rest of `hi.json`/`ar.json` (see below), called out separately here because Hindi's email translations were only statically validated this session (JSON well-formedness + key-existence), not live-rendered in a browser the way Arabic was — see §4.25's verification section for exactly what that does and doesn't cover. |
| `lib/validation.ts` | **Nothing — resolved, no work needed** | See Batch 5 finding above. Not translation-worthy in its current form; the underlying raw-error-array bug is a separate future engineering fix, not an i18n task. |
| Fix for the un-mounted `sonner` `<Toaster>` (14 files' toasts currently invisible) | **Engineering (future session)** | New, surfaced during Batch 5 — see note above. Not an i18n task; likely fix is either mounting `sonner`'s own `<Toaster>` somewhere in the tree, or migrating those 14 files to the already-working `useToast()`/`<Toaster />` system instead — an actual choice, not something to pick unilaterally during a translation pass. |
| Native-speaker review of `hi.json`/`ar.json` | **Boss / translator** | Recommended before production launch — see confidence note above. The ledger/accounting terminology from Batch 3b is the highest-priority item to check first, given both its specialized vocabulary and its direct connection to a vendor's own money. |
| RTL layout tolerance across the now-complete non-admin sweep | **Boss / design call** (resolved technically) | RTL mirroring has been proven across static pages, an interactive storefront flow, a large table-and-form-heavy admin-style UI (the entire vendor portal), a full auth/registration flow including numeric OTP/phone entry fields, HTML emails (§4.25), dense legal/policy pages (§4.26), authenticated account-management pages (§4.27), and finally category/shop/seller/blog pages, homepage components, and error/not-found pages (§4.28 — including Server Components' first appearance in the sweep, and asymmetric decorative background elements correctly mirrored vs. symmetric ones correctly left alone). No friction found anywhere with Tailwind v4's logical properties (web) or plain conditional CSS (email, which has no build-time class processing) across the entire non-admin surface. |
| Disposable verified test account for authenticated checkout flow | **Engineering (future session), if closing this gap is wanted** | Batch 2 could not live-verify the authenticated portion of `/checkout` (coupon apply, payment method, place order) — it correctly redirects unauthenticated sessions, and creating a real verified account requires a genuine OTP email round-trip, judged disproportionate for a UI-text/RTL check against the shared production database this pass. |
| Disposable vendor account (approved shop, active subscription) for authenticated vendor-portal verification | **Engineering (future session), if closing this gap is wanted** | Same class of gap as above, one level deeper — every `app/vendor/**` route requires an authenticated `shop_owner` session, so batches 3/3b's live checks could confirm routes load without runtime/translation errors and RTL attributes are correct, but could not visually confirm real rendered layout (bounding-box mirroring, ICU plurals against real data, status label maps against real backend values) the way Batch 2 could for the unauthenticated parts of the storefront. A static cross-check of all 664 `t()`/`t.rich()` calls across all 16 vendor-portal files against all three catalogs (0 missing keys) was used as the strongest available substitute across both batches — see §4.20/§4.21 for exactly what that does and doesn't prove. Batch 4 did not have this gap — every auth-form route is reachable unauthenticated, so full live browser + screenshot verification was possible there. Batch 5's `wishlist`/`vendor/reviews` toast work partially had this gap — the pages load unauthenticated fine, but the toasts themselves only fire behind an auth/data wall, so a static key cross-check was used for those specifically. |
| Fix for the stale "Instapeel" branding + missing logo image on `app/auth/forgot-password/page.tsx` | **Design / whoever owns current brand assets** | New, surfaced during Batch 4 — see note above. Not an i18n task; the correct fix is updating the branding copy and supplying a real logo file, not something to guess at during a translation pass. **Still open.** |

### Not blocked on Tap
Confirmed independent, as before — and now also proven technically, not just theoretically, across four passes: the library/RTL/pattern choices all work end-to-end without Tap being live.

---

## 4. Other business-side items surfaced during the audit (not yet actioned)

These aren't blocking any specific engineering task right now, but will need answers before a country actually goes live:

- **Support contact info per country** — is support email/phone shared across all countries for now, or does each country need its own? (Left as shared for now per earlier decision — revisit before launch.) **Update 2026-08-21:** these are now admin-editable DB fields (`PlatformSettings.supportEmail`/`supportPhone`, see `PROJECT_SOURCE_OF_TRUTH.md` §4.16), not an env var as this line previously implied — each country's separate database/deployment can already set its own values independently through the admin UI, no code change needed. This item is really just the *business* question (shared or per-country) now, not a technical blocker.
- **Domain/subdomain per country** — confirm `ae.linknsmile.com` / `qa.linknsmile.com` / `sa.linknsmile.com` (or whatever pattern) and get DNS set up when ready.
- **Tracking IDs per country** — Sentry, Facebook Pixel, GTM, Google Ads currently default to the existing India IDs (env-pluggable now, per Step 4 work) — decide whether each new country deployment gets its own tracking accounts or shares the existing ones.
- **Address model** — `lib/models/address.ts` (the saved address-book, separate from the checkout form) has no `country` field at all. Deliberately deferred during Step 5 — will need its own schema-change task before a UAE customer can save an address book entry properly, not just check out once.
- **Regional bank details for vendor payouts** — the existing IFSC (India) validation has a SWIFT/BIC path already available as an alternate — confirm this is sufficient for GCC vendor payouts or needs its own local bank-format validation.

---

## NOT Blocked — Can Continue Immediately

For clarity, since this document exists to *unblock* other work, not stall it:
- Any further storefront/vendor/admin feature work unrelated to payments, tax, or language (e.g. return/refund flow, shipping/courier integration, the outstanding security quick-fixes from the original audit — debug routes, `admin/images/*` auth, `/api/users` role bug)
- ~~The remaining i18n/RTL file sweep~~ — **complete as of 2026-08-25 (Batch 7b)**. The non-admin i18n/RTL sweep is done; only the deliberately-deferred admin panel remains
- Anything on the original feature brainstorm list not yet built